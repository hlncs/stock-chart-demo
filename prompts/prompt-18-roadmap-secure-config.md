# Prompt 18 – Secure Configuration

```text
Replace all hard-coded configuration.

Use environment variables.

Create

.env

.env.local

.env.production

Use Pydantic Settings.

Configuration includes

JWT secrets

Database path

Certificate paths

TLS settings

Allowed origins

Logging

Feature flags

Create example configuration files.

Validate configuration during startup.

Fail fast on invalid configuration.
```
